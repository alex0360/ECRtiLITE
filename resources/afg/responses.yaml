components:
  responses:
    RequestNotFound:
      description: Request not found
      content:
        application/json:
          schema:
            $ref: 'schemas.yaml#/components/schemas/RequestNotFound'
    DefaultError:
      description: Request not found
      content:
        application/json:
          schema:
            $ref: 'schemas.yaml#/components/schemas/DefaultError'